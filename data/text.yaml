# train and val datasets (image directory or *.txt file with image paths)
train: ../datasets/contest/annotations/train.txt
val: ../datasets/contest/annotations/val.txt

# number of classes
nc: 1

# class names
names: ['word']
